PORT=3000
JWT_SECRET=local-dev-secret

# Leave DB_URI empty for the bundled local SQLite database.
# Example MySQL URI:
# DB_URI=mysql://root:password@localhost:3306/formics
DB_URI=

# Optional overrides for local SQLite mode.
DB_DIALECT=sqlite
# SQLITE_STORAGE=./data/formics.sqlite
